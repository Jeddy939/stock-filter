# MoneyMaker ratings

This folder is the default local home for the central ratings database:

```text
central_stock_ratings.sqlite
```

The database is intentionally not committed to GitHub. Use
`EXPORT_RATING_ANALYSIS.bat` from the project folder to export TXT and CSV
analysis files when you want to inspect the ratings.
